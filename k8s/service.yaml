apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: default
spec:
  type: ClusterIP   # 生产建议改 LoadBalancer 或 Ingress
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: nginx
